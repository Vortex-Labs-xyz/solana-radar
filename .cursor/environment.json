{
  "install": "sudo apt-get update && sudo apt-get install -y python3-venv python3.13-venv && python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt && make lint && make test",
  "dockerComposeFile": "compose.yaml",
  "dockerComposeProfile": "ingest"
}
