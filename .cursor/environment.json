{
  "install": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && make lint && make test",
  "dockerComposeFile": "compose.yaml",
  "dockerComposeProfile": "ingest"
}